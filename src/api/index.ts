// import axios from "axios";

export const config = {
	// 默认请求地址
	baseURL: import.meta.env.VITE_API_URL,
	// 设置超时时间
	timeout: 10000,
	// 跨域时候允许携带凭证
	withCredentials: true
};

// class RequestHttp {

// }
